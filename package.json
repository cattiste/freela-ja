{
  "name": "freela-ja-web",
  "version": "1.0.0",
  "description": "Plataforma para conectar freelancers e estabelecimentos.",
  "author": "Bruno Cattiste",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^11.9.1",                    // Integração com Firestore, Auth, etc.
    "haversine-distance": "^1.2.4",           // Cálculo de distância entre dois pontos
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-helmet": "^6.1.0",                 // Gerenciamento de título e metadados das páginas
    "react-hot-toast": "^2.5.2",              // Sistema de notificações leve e moderno
    "react-icons": "^5.5.0",                  // Ícones prontos
    "react-input-mask": "^2.0.4",             // Máscara para inputs (telefone, CPF, etc.)
    "react-multi-date-picker": "^4.5.2",      // Calendário usado no painel (mantido esse)
    "react-router-dom": "^6.14.1"             // Rotas da aplicação
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.5.2",         // Plugin oficial React para Vite
    "autoprefixer": "^10.4.17",               // Compatibilidade de CSS
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.1",                  // Framework CSS utilitário
    "vite": "^5.0.10"                         // Empacotador leve e rápido
  },
  "engines": {
    "node": "18.x"
  }
}
